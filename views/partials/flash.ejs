<% if (flash) { %>
        <div class="mb-4 p-3 rounded <%= flash.type === 'error' ? 'bg-red-50 border border-red-100 text-red-800' : 'bg-green-50 border border-green-100 text-green-800' %> bg-green-50">
            <%= flash.message %>
        </div>
<% } %>